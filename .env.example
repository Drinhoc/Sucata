# ============================================
# Configurações do Sistema de Controle de Sucata
# ============================================

# Nome da aplicação (exibido no cabeçalho)
APP_NAME=Sistema de Controle de Sucata

# URL de conexão com o banco de dados PostgreSQL (OBRIGATÓRIO)
# No Railway: gerado automaticamente ao adicionar o plugin PostgreSQL
# Localmente: postgresql://usuario:senha@localhost:5432/sucata
DATABASE_URL=postgresql://usuario:senha@localhost:5432/sucata

# Segredo para assinar os tokens de sessão (OBRIGATÓRIO)
# Use uma string longa e aleatória — nunca compartilhe
# Gere uma: python -c "import secrets; print(secrets.token_hex(32))"
AUTH_COOKIE_SECRET=troque-por-uma-string-aleatoria-longa

# Duração da sessão persistente em dias (padrão: 7)
AUTH_DAYS=7

# ============================================
# Integração NF-e — Nuvem Fiscal API
# ============================================
# Credenciais OAuth2 da Nuvem Fiscal (https://www.nuvemfiscal.com.br)
# Obtenha em: Painel Nuvem Fiscal → API → Credenciais
# OBRIGATÓRIO para emissão de NF-e
NUVEM_FISCAL_CLIENT_ID=seu-client-id-aqui
NUVEM_FISCAL_CLIENT_SECRET=seu-client-secret-aqui

# Ambiente de emissão: "homologacao" (testes) ou "producao" (notas com validade fiscal)
# Pode ser sobrescrito pela configuração fiscal no painel admin
# NUVEM_FISCAL_AMBIENTE=homologacao
